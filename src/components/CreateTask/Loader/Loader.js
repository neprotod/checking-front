import React from 'react';
import LoaderComponent from 'react-loader-spinner';

const Loader = () => (
  <LoaderComponent type="ThreeDots" color="#ff6b08" height={18} width={100} />
);

export default Loader;
